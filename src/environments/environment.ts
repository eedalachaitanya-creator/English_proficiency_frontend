/**
 * Development environment config.
 * Used during `ng serve` and `ng build --configuration=development`.
 */
export const environment = {
  production: false,
     apiUrl: 'http://localhost:8000',
  // apiUrl: 'http://10.0.0.14:8013',
};